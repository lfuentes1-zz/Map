This project will convert a .yml file to a .json file.
Additionally, it will require the user to log in to an account and display
incidents on a google map based on the longitude and latitude location.